apiVersion: v1
kind: ConfigMap
metadata:
  name: ollive-config
  namespace: ollive
data:
  NODE_ENV: "production"
  PORT: "3001"
  FRONTEND_URL: "http://ollive-frontend:5173"
  DB_PATH: "/data/ollive.db"
